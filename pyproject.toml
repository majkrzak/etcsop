[project]
name = "etcsop"
version = "0.0.0"
dependencies = ["kopf", "pykube-ng", "jinja2"]
authors = [{ name = "Piotr Majkrzak", email = "piotr@majkrzak.dev" }]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-dir]
"etcsop" = "./src/"
